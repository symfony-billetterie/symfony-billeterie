{% block list %}
    {% if item.hasChildren and options.depth is not same as(0) and item.displayChildren %}
        {% import _self as knp_menu %}
        <ul{{ knp_menu.attributes(listAttributes) }} id="sidebar" class="nav nav-pills nav-stacked collapse in">
            {{ block('children') }}
        </ul>
    {% endif %}
{% endblock %}
