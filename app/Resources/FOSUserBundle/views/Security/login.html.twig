{% extends "app/layout.html.twig" %}

{% block content %}
    <form action="{{ path("fos_user_security_check") }}" method="post">
        <div class="panel col-md-6 col-md-offset-3">
            <div class="panel-heading">
                <div class="panel-title">{{ 'app.user.login.title'|trans }}</div>
            </div>
            <div class="panel-body">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>
                {% endif %}
                <div class="form-group">
                    <label for="username">{{ 'app.user.login.username'|trans }}</label>
                    <div class="input-group">
                        <span class="input-group-addon"><i class="fa fa-user"></i></span>
                        <input type="text" class="form-control" id="username" name="_username"
                               value="{{ last_username }}"
                               required="required"/>
                    </div>
                </div>
                <div class="form-group">
                    <label for="password">{{ 'app.user.login.password'|trans }}</label>
                    <div class="input-group">
                        <span class="input-group-addon"><i class="fa fa-lock"></i></span>
                        <input type="password" class="form-control" id="password" name="_password" required="required"/>
                    </div>
                </div>
                <div class="form-group">
                    <input type="checkbox" id="remember_me" name="_remember_me" value="on"/>
                    <label for="remember_me">{{ 'app.user.login.remember_me'|trans }}</label>
                </div>
            </div>
            <div class="panel-footer">
                <div class="row">
                    <div class="col-md-6">
                        <a class="btn btn-primary" href="{{ path('fos_user_resetting_request') }}">
                            {{ 'app.user.login.forgotten_password'|trans }}
                        </a>
                    </div>
                    <div class="col-md-6 text-right">
                        <input type="submit" class="btn btn-success" id="_submit" name="_submit"
                               value="{{ 'common.button.connection'|trans }}"/>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock content %}
